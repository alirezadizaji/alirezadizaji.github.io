name: Trigger Dark Mode

on:
  schedule:
    - cron: '2 * * * *'
  
  workflow_dispatch:

jobs:
  init:
    runs-on: ubuntu-latest

    outputs:
      trigger_darkmode: ${{ steps.trigger-darkmode.outputs.data }}
      hour: ${{ steps.current-time.outputs.hour }}

    steps:
    - name: Checkout 🛎️
      uses: actions/checkout@v3
    
    - name: Get trigger darkmode variable
      uses: jbutcher5/read-yaml@1.6
      id: trigger-darkmode
      with:
        file: '_config.yml'
        key-path: '["trigger_darkmode"]'

    - name: Get current time
      uses: josStorer/get-current-time@v2.0.2
      id: current-time

  trigger-darkmode:
    runs-on: ubuntu-latest
    needs: init
    env:
      TRIGGER_TIME: ${{ needs.init.outputs.trigger_darkmode }}
      LIGHT_KEY: start_light
      DARK_KEY: start_dark
      HOUR: ${{ needs.init.outputs.hour }}

    if: ${{ needs.init.outputs.trigger_darkmode != null }}
    steps:
    - name: Checkout 🛎️
      uses: actions/checkout@v3

    - name: Disable dark mode
      if: ${{ env.HOUR >= env.TRIGGER_TIME[env.LIGHT_KEY] && env.HOUR <= env.TRIGGER_TIME[env.DARK_KEY] }}  
      uses: fjogeleit/yaml-update-action@main
      with:
        valueFile: '_config.yml'
        propertyPath: 'darkmode'
        value: "!!bool 'false'"
        commitChange: true
        message: 'Disable Dark Mode.'
    
    - name: Enable dark mode
      if: ${{ env.HOUR < env.TRIGGER_TIME[env.LIGHT_KEY] || env.HOUR > env.TRIGGER_TIME[env.DARK_KEY] }}  
      run: echo $TRIGGER_TIME[$LIGHT_KEY]
      #uses: fjogeleit/yaml-update-action@main
      #with:
       # valueFile: '_config.yml'
       # propertyPath: 'darkmode'
       # value: "!!bool 'true'"
       # commitChange: true
       # message: 'Enable Dark Mode.'
